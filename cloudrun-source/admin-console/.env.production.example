# Admin console production env.
# Keep API same-origin; nginx proxies /api to the Node.js backend.
VITE_API_BASE=/api

# Dev proxy only. Production build does not use this value in the browser.
VITE_API_TARGET=http://127.0.0.1:3000
